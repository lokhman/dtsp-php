// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("dtsp", "for dtsp support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("dtsp", "enable dtsp support", "no");

if (PHP_DTSP != "no") {
	EXTENSION("dtsp", "dtsp.c");
}

